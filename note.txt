You are absolutely right to ask about this! Managing the raw data is important.

If you use SQLite, you usually need a small tool like DB Browser for SQLite to see the raw tables. But if you want to avoid installing separate software, I can build a "Database Manager" page directly into your application.

This would be a special hidden Admin link where you can:

View all tables (Products, Sales, etc.) just like an Excel sheet.
Edit or Delete any specific row manually if something goes wrong.
Run SQL commands if needed.
My recommendation is still SQLite because:

It makes the app fast and portable.
We don't need to install a heavy database server like MySQL.
I will solve the "editing" problem by building the management tool inside your app.
Does this plan work for you?

I will convert your data to SQLite.
I will build a hidden Admin Data Manager inside the app so you can control everything easily.